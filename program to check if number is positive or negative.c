//program to show if number is positive or negative
#include <stdio.h>
int main()
{
	int number;
	printf("enter any number \n");
	scanf("%d",&number);
	if(number=1)
	{
		printf("%d the number is positive",number);
	}
	else
	{
		printf("%d the number is negative",number);
	}
	return 0;
}
